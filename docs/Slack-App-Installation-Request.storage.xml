<ac:structured-macro ac:name="info">
  <ac:rich-text-body>
    <p><strong>Before you publish:</strong> replace every <code>&lt;PLACEHOLDER&gt;</code> on this page, then delete this panel.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<table>
  <colgroup><col style="width: 220.0px;"/><col style="width: 520.0px;"/></colgroup>
  <tbody>
    <tr><th>App name</th><td>Support Tickets</td></tr>
    <tr><th>Status</th><td><ac:structured-macro ac:name="status"><ac:parameter ac:name="colour">Yellow</ac:parameter><ac:parameter ac:name="title">PENDING IT / SECURITY APPROVAL</ac:parameter></ac:structured-macro></td></tr>
    <tr><th>Requested by</th><td>&lt;YOUR NAME&gt;, &lt;TEAM&gt;</td></tr>
    <tr><th>Business owner</th><td>&lt;MANAGER / TEAM LEAD&gt;</td></tr>
    <tr><th>Workspace</th><td>&lt;SLACK WORKSPACE&gt;</td></tr>
    <tr><th>Install scope</th><td>Single workspace, bot user only</td></tr>
    <tr><th>Permissions requested</th><td><code>commands</code>, <code>chat:write</code>, <code>files:read</code></td></tr>
    <tr><th>Systems touched</th><td>Slack, Google Apps Script (our Workspace tenant), Freshdesk</td></tr>
    <tr><th>New vendors introduced</th><td>None — all three are already in use</td></tr>
    <tr><th>Date raised</th><td>&lt;DATE&gt;</td></tr>
  </tbody>
</table>

<ac:structured-macro ac:name="toc">
  <ac:parameter ac:name="maxLevel">2</ac:parameter>
  <ac:parameter ac:name="minLevel">1</ac:parameter>
</ac:structured-macro>

<h1>At a glance</h1>

<p>We are asking for approval to install a custom, internally built Slack app called <strong>Support Tickets</strong> into the &lt;SLACK WORKSPACE&gt; workspace.</p>

<p>It adds a <code>/newticket</code> command that opens a short form. On submit, it creates a ticket in our existing Freshdesk account and DMs the requester their ticket number. That is the entire feature set.</p>

<ac:structured-macro ac:name="note">
  <ac:rich-text-body>
    <p>It requests <strong>three</strong> bot permissions. It reads no messages, no channel lists and no user directory, and stores no Slack data beyond what is needed to file and log one ticket.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<p>The app is built and working in a personal development workspace. This request is to move it into the company workspace so the support team can use it.</p>

<h2>What we need from you</h2>
<ol>
  <li>Approval to create and install the app with the three scopes listed under <em>Permissions requested, and why</em>.</li>
  <li>An administrator to perform the installation, if workspace policy requires it. We will provide the app manifest and can walk through the configuration.</li>
  <li>Confirmation of any <strong>log retention period</strong> we should apply to the Google Sheet.</li>
</ol>

<h1>Why we want it</h1>
<ul>
  <li>Support requests currently arrive in Slack as messages and get lost. There is no queue, no ownership, and no record of what was asked or when.</li>
  <li>Asking people to leave Slack and use the Freshdesk portal is the reason they do not raise tickets in the first place. Meeting them where they already are is the whole point.</li>
  <li>Every request becomes a tracked Freshdesk ticket with an owner and an SLA, and the requester gets a ticket number immediately rather than wondering whether anyone saw their message.</li>
  <li>Submissions are logged to a Google Sheet, so we can see volume, response patterns and failures without querying Freshdesk.</li>
</ul>

<h1>What the app actually does</h1>

<table>
  <colgroup><col style="width: 150.0px;"/><col style="width: 590.0px;"/></colgroup>
  <tbody>
    <tr><th>Step</th><th>What happens</th></tr>
    <tr><td><strong>1. Open</strong></td><td>A user runs <code>/newticket</code>, or clicks a "Create a ticket" button posted in a support channel.</td></tr>
    <tr><td><strong>2. Form</strong></td><td>Slack shows a short form: subject, product, summary and details are required; session or build IDs, steps to reproduce and file attachments are optional. It also asks for the requester's email address, and optionally addresses to copy on the ticket.</td></tr>
    <tr><td><strong>3. Submit</strong></td><td>Slack sends the form to our Google Apps Script endpoint. It validates the input, records the submission, and replaces the form with a "Request received" confirmation. No other system is contacted at this point.</td></tr>
    <tr><td><strong>4. Create</strong></td><td>A background job creates the ticket in Freshdesk over its REST API and DMs the requester their ticket number.</td></tr>
    <tr><td><strong>5. Attach</strong></td><td>If files were attached, the same job downloads them from Slack and sends them to Freshdesk with the ticket, so the ticket is complete when it is created.</td></tr>
  </tbody>
</table>

<p>The app does <strong>not</strong> read channel history, does <strong>not</strong> respond to messages, does <strong>not</strong> subscribe to any Slack events, and has no Home tab.</p>

<h1>How it is built</h1>

<p>There is no new infrastructure and no new vendor. The backend is a Google Apps Script web app running inside our own Google Workspace tenant.</p>

<table>
  <colgroup><col style="width: 200.0px;"/><col style="width: 540.0px;"/></colgroup>
  <tbody>
    <tr><th>Component</th><th>Detail</th></tr>
    <tr><td>Slack app</td><td>Custom app, bot user only. Not distributed, not on the Slack Marketplace, not org-wide.</td></tr>
    <tr><td>Backend</td><td>Google Apps Script web app, owned by a company Google account, in our Workspace tenant.</td></tr>
    <tr><td>Ticketing</td><td>Our existing Freshdesk account, via its documented REST v2 API.</td></tr>
    <tr><td>Logging</td><td>A Google Sheet in our Workspace, shared with the support team only.</td></tr>
    <tr><td>Hosting cost</td><td>None. No servers, containers or third-party hosting are involved.</td></tr>
    <tr><td>Source code</td><td>Held in our Git repository and reviewed like any other internal code.</td></tr>
  </tbody>
</table>

<h2>Data flow</h2>

<ac:structured-macro ac:name="code">
  <ac:parameter ac:name="theme">Confluence</ac:parameter>
  <ac:plain-text-body><![CDATA[Slack  ->  Google Apps Script (our tenant)  ->  Freshdesk API
                     \->  Google Sheet (our tenant)]]></ac:plain-text-body>
</ac:structured-macro>

<p>All three endpoints are systems we already use and already have agreements with. No data is sent anywhere else, and nothing crosses into a system that is new to the company.</p>

<h1>Permissions requested, and why</h1>

<p>The app calls exactly <strong>three</strong> Slack API methods across its whole lifetime: <code>views.open</code> (show the form), <code>chat.postMessage</code> (DM the requester) and <code>auth.test</code> (a setup-time health check). The permission list is derived from those three calls and nothing else.</p>

<table>
  <colgroup><col style="width: 150.0px;"/><col style="width: 200.0px;"/><col style="width: 390.0px;"/></colgroup>
  <tbody>
    <tr><th>Scope</th><th>What it allows</th><th>Why this app needs it</th></tr>
    <tr>
      <td><code>commands</code></td>
      <td>Register and receive the <code>/newticket</code> slash command.</td>
      <td>This is how a user opens the form. Slack sends us the command text, the user ID and the channel ID; it grants no access to anything else.</td>
    </tr>
    <tr>
      <td><code>chat:write</code></td>
      <td>Post messages as the bot.</td>
      <td>Used to DM the requester their ticket number, and to post the optional "Create a ticket" button into a support channel the bot has been invited to. It does not permit reading any message.</td>
    </tr>
    <tr>
      <td><code>files:read</code></td>
      <td>Download files a user attaches to the form.</td>
      <td>Required by Slack's file upload form element. Without it the form still submits, but Slack returns a sign-in page instead of the file and no attachment reaches Freshdesk.</td>
    </tr>
  </tbody>
</table>

<h2>Permissions we deliberately did not request</h2>

<p>We reviewed the code against the scope list and removed three scopes that were in an earlier draft but are never used. Noted here so the review has the full picture of what was considered.</p>

<table>
  <colgroup><col style="width: 190.0px;"/><col style="width: 200.0px;"/><col style="width: 350.0px;"/></colgroup>
  <tbody>
    <tr><th>Scope</th><th>Would have allowed</th><th>Why it is not needed</th></tr>
    <tr>
      <td><code>channels:read</code><br/><code>groups:read</code></td>
      <td>Listing channels and reading their metadata.</td>
      <td>The channel ID and name already arrive inside the slash command and interactivity payloads. We never call <code>conversations.info</code>, so we never need to read channel lists.</td>
    </tr>
    <tr>
      <td><code>users:read</code><br/><code>users:read.email</code></td>
      <td>Reading the user directory and members' email addresses.</td>
      <td>The requester types their own email into the form. We never look anyone up in the directory.</td>
    </tr>
    <tr>
      <td><code>chat:write.public</code></td>
      <td>Posting into channels the bot has not joined.</td>
      <td>The bot only sends DMs and posts into channels it has been explicitly invited to.</td>
    </tr>
  </tbody>
</table>

<ac:structured-macro ac:name="info">
  <ac:parameter ac:name="title">On files:read</ac:parameter>
  <ac:rich-text-body>
    <p>This is the broadest of the three, so it is worth being precise. The scope permits reading file content and metadata for files the app can see. In practice the app only ever downloads files a user has just attached to its own form, using the URL Slack hands us in that submission, and it does so once — the file is streamed straight to Freshdesk and never written to disk or to the log.</p>
    <p>If your review would prefer the app not handle attachments at all, we can remove the upload field and drop this scope entirely. Requesters would then attach screenshots by replying to the Freshdesk email instead. We would rather keep it, because a support ticket without the screenshot usually costs a round trip.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<h1>What data is handled, and where it goes</h1>

<table>
  <colgroup><col style="width: 200.0px;"/><col style="width: 250.0px;"/><col style="width: 290.0px;"/></colgroup>
  <tbody>
    <tr><th>Data</th><th>Where it goes</th><th>Retention</th></tr>
    <tr><td>Subject, product, summary, details, steps</td><td>Freshdesk ticket</td><td>Per existing Freshdesk retention policy.</td></tr>
    <tr><td>Requester email address, and any CC addresses</td><td>Freshdesk ticket, and the Google Sheet log</td><td>Typed by the requester. Kept in the log for support reporting.</td></tr>
    <tr><td>Slack user ID, username, channel name</td><td>Google Sheet log only</td><td>Identifies who raised the request and from where. Deliberately not written onto the ticket itself, which the requester can read.</td></tr>
    <tr><td>Attached files</td><td>Freshdesk ticket</td><td>Passed straight through. Never stored by the integration.</td></tr>
    <tr><td>Ticket description text</td><td>Google Apps Script property store, briefly</td><td>Deleted the moment the ticket exists — normally within seconds.</td></tr>
  </tbody>
</table>

<ac:structured-macro ac:name="note">
  <ac:parameter ac:name="title">Ticket descriptions are deliberately kept out of the log</ac:parameter>
  <ac:rich-text-body>
    <p>The Google Sheet records the <em>character count</em> of each description, never the text. Ticket bodies routinely contain customer detail, and a spreadsheet has a much wider and less controlled access surface than Freshdesk does.</p>
    <p>The one exception is the few seconds between a form being submitted and the ticket being created, when the text sits in the Apps Script property store so the background job can pick it up. It is deleted as soon as the ticket exists.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<p>The Google Sheet log is append-only and is currently kept indefinitely. <strong>If your team has a standard retention period for operational logs containing email addresses, tell us what it is and we will apply it.</strong></p>

<h1>Security model</h1>

<p>The Apps Script web app must be reachable by Slack, which on this platform means it is deployed as publicly addressable. The controls below exist because of that.</p>

<table>
  <colgroup><col style="width: 200.0px;"/><col style="width: 540.0px;"/></colgroup>
  <tbody>
    <tr><th>Control</th><th>Implementation</th></tr>
    <tr><td>Transport</td><td>HTTPS only, end to end. Google, Slack and Freshdesk all enforce TLS.</td></tr>
    <tr><td>Endpoint authentication</td><td>A 32-byte random shared secret is required in the request URL. It is checked before any part of the request is parsed. Requests without it are dropped silently.</td></tr>
    <tr><td>App identity</td><td>The Slack application ID on every request must match ours.</td></tr>
    <tr><td>Credential storage</td><td>The Slack bot token and Freshdesk API key are held in Apps Script Script Properties. They are never in source code and never in the Git repository.</td></tr>
    <tr><td>Freshdesk access</td><td>A standard <strong>agent</strong> API key, not an administrator key. It can create tickets; it cannot administer the account.</td></tr>
    <tr><td>Duplicate protection</td><td>Repeated submissions of the same form are suppressed, so a retry or a double click cannot file the same ticket twice.</td></tr>
    <tr><td>Response surface</td><td>The endpoint returns no data. A caller learns nothing from it.</td></tr>
  </tbody>
</table>

<h2>Known limitation: Slack request signatures cannot be verified</h2>

<ac:structured-macro ac:name="warning">
  <ac:parameter ac:name="title">We are raising this ourselves rather than leaving your team to find it</ac:parameter>
  <ac:rich-text-body>
    <p>Slack signs every request with an <code>X-Slack-Signature</code> header. <strong>Google Apps Script does not expose HTTP request headers to server-side code</strong>, so that signature cannot be read and the standard verification step is not available on this platform.</p>
    <p>The shared secret in the URL stands in for it. The practical consequence is that <strong>the deployment URL is itself a credential</strong>: anyone holding the full URL could submit a forged form and cause a Freshdesk ticket to be created.</p>
    <p><strong>Worst case</strong> is nuisance ticket creation with a spoofed requester email address. It grants no read access to Slack, no read access to Freshdesk, and no route to any other system. The URL is treated as a secret, is not committed to the repository, and can be rotated in about two minutes.</p>
    <p><strong>If you consider that residual risk unacceptable</strong>, the fix is well understood: put a small signature-verifying proxy in front of the endpoint so the real Slack signature is checked before anything reaches Apps Script. We are happy to build that if it is a condition of approval — say so and we will treat it as in scope.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<h2>Who can access what</h2>
<ul>
  <li>Edit access to the Apps Script project will be limited to <strong>two named people</strong>. Anyone with edit access can read the stored credentials, so this list is kept deliberately short and reviewed if someone changes team.</li>
  <li>The project is owned by a <strong>company account</strong>, not a personal one.</li>
  <li>The Google Sheet log is shared with the support team only.</li>
  <li>The Slack app is installed by the workspace administrator and can be uninstalled by them at any time without our involvement.</li>
</ul>

<h1>Proposed rollout</h1>

<table>
  <colgroup><col style="width: 100.0px;"/><col style="width: 280.0px;"/><col style="width: 360.0px;"/></colgroup>
  <tbody>
    <tr><th>Phase</th><th>Scope</th><th>Exit criteria</th></tr>
    <tr><td>1</td><td>Support team only, one channel</td><td>~20 tickets filed. Every one appears in Freshdesk and in the log, with no duplicates.</td></tr>
    <tr><td>2</td><td>Internal teams that raise support requests</td><td>One week with no failures needing manual repair.</td></tr>
    <tr><td>3</td><td>All internal channels where support is requested</td><td>—</td></tr>
  </tbody>
</table>

<ac:structured-macro ac:name="tip">
  <ac:parameter ac:name="title">Rollback</ac:parameter>
  <ac:rich-text-body>
    <p>Uninstalling the Slack app stops the integration immediately and completely. Tickets already created are unaffected, and no Slack or Freshdesk configuration needs to be undone. There is nothing to migrate and nothing left running.</p>
  </ac:rich-text-body>
</ac:structured-macro>

<h1>Appendix A — Every Slack API call the app makes</h1>

<table>
  <colgroup><col style="width: 200.0px;"/><col style="width: 160.0px;"/><col style="width: 380.0px;"/></colgroup>
  <tbody>
    <tr><th>Method</th><th>Scope used</th><th>Purpose</th></tr>
    <tr><td><code>views.open</code></td><td>none required</td><td>Display the ticket form after <code>/newticket</code> or a button click.</td></tr>
    <tr><td><code>chat.postMessage</code></td><td><code>chat:write</code></td><td>DM the requester their ticket number and the attachment result.</td></tr>
    <tr><td><code>auth.test</code></td><td>none required</td><td>Setup-time check that the token works. Not called during normal use.</td></tr>
    <tr><td>file download</td><td><code>files:read</code></td><td>Fetch a file the user attached to the form, once, to forward to Freshdesk.</td></tr>
  </tbody>
</table>

<p>There are no other calls, no Slack Events API subscriptions, and no background polling of Slack.</p>

<h1>Questions</h1>

<p>Happy to walk through the code, the Slack app configuration or the data flow with your team, and to make changes as a condition of approval.</p>

<p><strong>Contact:</strong> &lt;YOUR NAME&gt; — &lt;EMAIL&gt;</p>
